name: Apply Vanita trend label hardening

on:
  push:
    branches:
      - qa/launch-readiness-hardening-v1

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: qa/launch-readiness-hardening-v1
          fetch-depth: 0
      - run: |
          npm --prefix projects/vanita-stock run harden
          npm --prefix projects/vanita-stock run check
      - run: |
          git config user.name "BDB OS Release Bot"
          git config user.email "actions@users.noreply.github.com"
          git rm .github/workflows/apply-vanita-trend-label.yml
          git add projects/vanita-stock/index.html
          git commit -m "Remove fixed Vanita trend claim"
          git push origin HEAD:qa/launch-readiness-hardening-v1
